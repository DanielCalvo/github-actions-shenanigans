name: learn-github-actions
on: [push]
jobs:
  check-bats-version:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "hello world"
          echo "can I do a multiline script this way?"
          date
          whoami
          cat /etc/passwd